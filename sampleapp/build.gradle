apply plugin: 'com.android.application'

dependencies {
    api project(':sdk')
}

android {
    compileSdkVersion 27
    buildToolsVersion '26.0.2'

    defaultConfig {
        minSdkVersion 14
    }
}
